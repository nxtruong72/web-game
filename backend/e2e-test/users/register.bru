meta {
  name: register
  type: http
  seq: 1
}

post {
  url: {{host}}/b2b/users
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "{{admin_password}}",
    "phone": "{{admin_phone}}",
    "email": "{{admin_email}}"
  }
}
